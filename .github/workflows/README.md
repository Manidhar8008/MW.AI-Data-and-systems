# GitHub Workflows

CI workflows can be added here after the first backend and frontend test commands are stable.

Keep initial CI simple:

- install backend dependencies
- run backend tests
- install frontend dependencies
- run TypeScript build

