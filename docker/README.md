# Docker

Docker assets will live here when the project is ready for containerized local development.

The initial MVP should remain runnable with direct Python and Node commands before adding Docker Compose.

